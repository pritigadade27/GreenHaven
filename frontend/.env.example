# Where the deployed backend lives, with no trailing slash.
# Leave this UNSET for local development — the Vite proxy handles /api and no
# CORS is involved. Set it only in the hosting provider's build settings.
VITE_API_BASE_URL=https://your-api.onrender.com
